# 「唱一句 · 定调子」功能说明（供 V0 生成新界面用）

> 本文件只描述**产品功能、数据流与引擎契约**，不包含任何 UI 设计约束——视觉风格、组件形态、配色排版完全由你决定。
> 设计一个你认为是「最好的音乐学习工具」的样子，不需要参考现有实现的外观。

## 一、产品是什么

**一句话**：用户对着手机哼一句副歌（2~15 秒），立刻得到：这是什么调、变调夹夹几品、按什么和弦、扫什么节奏型、以及这句旋律的简谱。

**目标用户**：害怕乐理、只会按 C/G 几个和弦的吉他新手。他们想弹喜欢的歌，但不会定调、看不懂谱、不敢开口。

**核心价值**：
- 零乐理门槛：所有专业概念都翻译成大白话
- 纯本地：录音不上传云端（可选本地高精度模式）
- 陪伴感：有一个音乐伙伴角色全程给情绪反馈，不是冰冷的工具

**主场景**：手机（移动端优先），桌面可用。

## 二、核心流程（3 步）

1. **按住录音**（2~15 秒，哼一句副歌）→ 松开
2. **自动分析**（约 1~2 秒）
3. **结果呈现**：调性（大字）+ 变调夹位置 + 左手和弦按法 + 右手节奏型 + 教材式简谱

## 三、功能清单（按场景）

### 测调页（首页）
- **录音交互**：按住按钮录音、松开分析；向上滑超过一定距离取消本次录音；录音时有实时反馈（当前音高/唱名/音量/时长）
- **首次引导**：第一次使用，伙伴角色用 2~3 句话介绍自己和使用方法（可跳过，仅一次）
- **今日一调**：每天轮换一个调 + 一首公版旋律（小星星/两只老虎/生日快乐/欢乐颂），可听示范跟唱；当天完成过测试自动打勾

### 分析过程
- 约 1~2 秒的等待状态（有进度感）

### 结果页
- **调性结果**：调性名（大字）+ 大小调 + 置信度 + 一句情绪描述（大调偏明亮、小调偏温柔，按日期轮换）
- **备选调提示**：如「大概率 B 小调 67%，也可能是 D 大调」
- **变调夹**：夹几品 + 用哪套指法；不夹时说明
- **「怎么听出来的」**：大白话解释判定依据；专业数据默认折叠，可展开
- **简谱**：教材式完整记谱（数字 + 八分减时线 + 拖拍增时线 + 附点 + 小节线 + 休止符 + 三连音 + 调外音升降号），显示拍号与速度；点任意数字试听该音；量化不确定的音符有弱化标记；整段节奏过乱时退回简单排版并提示
- **怎么弹（两页横滑）**：
  1. 左手·和弦与按法：按用户旋律实时配的和弦走向 + 备选走向模板
  2. 右手·节奏型：30 种（中文教材式记谱如「根三二三」），按情绪分两组（欢快/抒情），可换一批、可试听，试听速度跟随用户哼唱速度
- **回放**：听自己的原声 / 听旋律（乐器音色还原真实节奏与重音）/ 下载回放 / 音色切换（尼龙弦/钢弦/钢琴）
- **多段补充**：再哼一段（最多 5 段）合并提高准确率，显示每段倾向与置信度变化
- **分享**：生成一张分享卡片图下载
- 底部主行动：「再测一次」+「分享」

### 练习页
- **音准闯关**：8 关（C4~C5），对着麦克风唱出目标音，实时音高曲线，±50 音分内累计 1.2 秒过关；过关有正向反馈；每关解锁一个收集品
- **猜调挑战（双人小游戏）**：播放随机调的神秘旋律，两人按 12 个候选调竞猜

### 我的页
- **连续陪伴**：连续第 2 天「你昨天也来过」，超 7 天「好久不见」；有连续天数奖励
- **调性收集册**：12 个调的徽章，测出过的点亮，点徽章筛选历史记录
- **历史记录**：默认折叠
- **设置**：专业模式（小调显示方式切换）/ 专家判据开关 / 回放音色 / AI 助手开关 / 关于

### 全局伙伴角色（功能层面）
- 一个音乐伙伴角色贯穿全产品，职责：
  1. **情绪反馈**：录音时倾听、分析时等待、结果时庆祝、失败时鼓励、低置信时诚实
  2. **AI Agent**：右下角常驻入口，可对话；它能把分析结果翻译成通俗生动的话（例：「音准偏差 +15 音分」→「刚才那个音稍微高了一丢丢，就像唱歌时踮了一下脚尖～」），也能回答产品使用问题；对话有记忆（记住用户测过的调、进度等）
- **诊断面板**：角落小面板显示运行状态（版本/模式/采样/最近回放/量化统计），用于问题排查

## 四、引擎契约（重要）

**所有音乐逻辑由 `humkey-core.js` 提供，界面只负责调用与呈现，禁止重新实现以下任何一项**（它们经过大量真实设备与真实人声调试，重新实现会引入已解决过的 bug）：

| 能力 | 接口（以最终文档为准） | 说明 |
|---|---|---|
| 录音/转录 | `core.record.start() / stop()` | 麦克风采集 + 音高转录（本地 YIN，无云端） |
| 定调 | `core.analyze(notes)` | 输出 {调性, 置信度, 备选调, 证据} |
| 简谱生成 | `core.jianpu.build(notes, key)` | 输出量化谱面符号流（含拍号/速度/诊断字段） |
| 和弦/节奏 | `core.harmony.chords(...)` / `core.harmony.rhythms(...)` | 12 和弦库 + 30 节奏库 |
| 变调夹 | `core.guitar.fingering(key)` | 变调夹品数 + 指法 |
| 播放 | `core.play.melody(...)` / `core.play.tone(midi)` / `core.play.score(...)` | 乐器音色回放（含真实节奏还原） |
| 高精度(可选) | `POST /transcribe`（本地 FastAPI 后端） | 可选的高精度转录与主音引擎 |
| 持久化 | localStorage（约定前缀 hk*） | 历史/徽章/设置/对话记忆 |

界面只需：组织数据渲染、管理页面流程、处理权限申请（麦克风）、调用上述接口。

## 五、功能级约束（非设计约束）

1. 核心链路（录音→分析→结果→播放）**必须无云端依赖可用**；AI 对话部分可依赖云 API（可选功能）
2. 麦克风权限被拒绝时要有清晰的引导与重试路径
3. 所有用户数据只存本地（localStorage），不要求账号
4. 移动端是主场景：单手操作、竖屏、点击区足够大
5. 音频播放要适配手机静音开关与自动播放策略（首次交互后才能出声）
6. 页面切换不打断正在进行的录音/播放

## 六、验收口径（功能正确性）

- 同一段录音，界面如何换，输出的调性/简谱/和弦/变调夹必须一致（引擎是唯一事实源）
- 12 个调的标注集：主音判定 ≥ 90% 正确（已有回归集可验证）
- 播放：每个音都可闻、节奏与录音一致
