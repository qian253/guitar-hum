# 哼唱定调 · 核心技术白皮书(供外部讨论用)

## 产品场景
用户按住按钮哼唱流行歌副歌 2~15 秒(通常 4~8 秒),系统实时给出:调性(主音+大小调)、变调夹位置、和弦走向、节奏型。核心用户是**怕乐理的新手**,人声为单音旋律,音准有误差、有滑音/气息毛刺。

## 当前技术流水线

### 1. 预处理(preprocess.py)
解码 22050Hz 单声道 → 去直流 → 4 阶高通 70Hz(滤嗡声,不伤人声基频 ≥98Hz)→ 峰值归一化 0.85 → 轻量噪声门限 -48dBFS

### 2. 音高转录(三档自动降级)
- **主**:basic-pitch(Spotify,ICASSP 2022,ONNX 本地推理),输出音符事件 (start, end, pitch_midi, amplitude, pitch_bends)
- **备**:torchcrepe(CREPE F0 + 分段)
- **前端降级**:YIN(CMNDF,44.1k/2048 窗 + 滞回分段)

### 3. 音符清洗(note_cleaner.py)
①滑音合并:相邻半音且任一 <120ms → 合并,音高取时值长者
②极短音 <100ms 删除(阈值随旋律时值中位数自适应收缩,只收紧不放松)
③跳跃毛刺:<150ms 且(两侧均 ≥ 小三度 或 一侧 ≥3 半音且前后音平滑 ≤2)→ 删除
④振幅 < 均值 50% 删除(basic-pitch 无逐音置信度,用 amplitude 代理)

### 4. 八度修正(octave_fix.py)
孤立八度跳变折回:前后邻音互差 ≤6 半音,且当前音距邻 >6 半音、折 ±12 后 ≤4 半音 → 折回(保守,不误修正常大跳)

### 5. 主音检测引擎(tonic_engine.py)——核心
**音级直方图**:时长权重 log2(1+dur) 压缩(防长尾音独大)+ 伪计数平滑 0.5
**根音打分(12 候选)**:
```
score(root) = max over {major, minor} of [
    Pearson(hist, Krumhansl-Schmuckler 1982 profile 循环移位)      // K-S 相关
  + 0.20 × 音阶成员占比(旋律落在该调 7 声音阶内的时长比例)
]
+ 0.10 × 稳定性点积(hist·profile 归一化)
+ 0.15 × 结束音命中(最后音 ≥0.5s 且 = 根音)
+ 0.02 × 吉他友好调先验(C/G/D/A/E/F 大调、Am/Em/Bm/Dm 小调)
```
**动态权重**:音符 ≤5 个时 音阶成员 +0.10、稳定性 ×0.6、结束音 +0.05;≥14 个时 音阶成员 -0.05
**两步解耦**:先锁根音(大小调取较高分,模式无关),再在**同根音内**定大小调(大三度 vs 小三度时长、三和弦骨架、导音半音解决次数;小调须明显占优 ×1.25 才判小调)——禁止关系大小调反向改主音
**转调检测**:按时间中点分两段各自快速定根音,两段根音不同且各自置信度足够 → 标记调制、整体置信度 ×0.8、整体判后段目标调
**和弦验证**(轻量):和弦模板匹配(I ii iii IV V vi vii°,小调含大三 V),首尾落主和弦 + 主和弦时长占比 ≥25% → 置信度 +0.05
**置信度**:top1-top2 分差 / 0.18,封顶(短旋律 0.7、大小调模糊 0.6)
**辅助证据(只展示不计分)**:Chew 螺旋数组(partitura 公式:五度圈螺旋 + CE 中心效应点 + 大小调三元组模板)

### 6. 多段合并
同一首歌可再哼 ≤5 段,每段单独定调,合并后重判(合并时结束音不加权)

## 验证基准
- 168 条合成旋律(24 调 × 7 模式:音阶/琶音/流行/跑音/非主音结尾/极短 4 音/经过音):**主音 100%,调性精确 92.9%**
- 唯一系统性弱点:不含三度的极短旋律调式不可判定(主音仍对)——信息论边界

## 已知局限(想讨论的开放问题)
1. K-S profile 是为完整古典作品设计的,对 2~8 秒人声哼唱的适配性存疑(对比过 Temperley/Albrecht-Shanahan profile,168 基准上无差异)
2. 关系大小调(同一自然音集)仅靠音级直方图**本质上不可分**,目前靠三度/导音/结束音弱线索
3. 真实人声准确率尚无标注数据(正在自建:标注模式+参考音阶+原始音符导出)
4. 置信度未做校准(显示 62% 不等于真实正确率 62%)
5. 纯符号化管线(音符→直方图→模板匹配),没有用原始频谱特征,也没有序列级(音符先后顺序)建模

## 曾调研过、已否决/搁置的方案
- CREPE 转录:误差 6.1 音分 vs basic-pitch 0.0 → 只做备选
- 后端 madmom CNN 调性识别(P1-2 待做)、Essentia KeyExtractor(前端已做跨校验展示,不参与打分)
- 换 K-S profile(Temperley 更差 78%)
- 结束音一票否决(过拟合,已改弱加权)
- Chew 螺旋数组参与打分(短哼唱上只加噪,已归零,仅展示)
